import type { DiffResult, Snapshot } from '../model/types'

export function diffSnapshots(_left: Snapshot, _right: Snapshot): DiffResult {
  throw new Error('diffSnapshots() is not implemented yet.')
}
